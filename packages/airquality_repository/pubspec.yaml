name: airquality_repository
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.3

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  equatable: ^2.0.5
  json_annotation: ^4.6.0
  airquality_api:
    path: ../airquality_api

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  build_runner: ^2.0.0
  coverage: ^1.0.3
  json_serializable: ^6.3.1
  mocktail: ^0.3.0
